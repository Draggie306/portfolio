{
  "compilerOptions": {
    "target": "es2024",
    "module": "CommonJS",
    "sourceMap": true,
    "moduleDetection": "force"
  },

  // Don't include worker code
  "include": [
    "assets/**/*.ts"
  ],
}